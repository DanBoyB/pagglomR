# gen_costs <- read.table(path)
#
# gen_costs <- head(gen_costs[c(1:2, 4)])
#
# names(gen_costs) <- c("o_zone", "d_zone", "gen_cost")

#
# if(ncol(gen_costs) > 3) {
#     stop(paste0(
#         "Generalised costs should be specified in the following format ",
#         "(3 columns): Origin Zone, Destination Zone, Generalised Costs"))
# }
